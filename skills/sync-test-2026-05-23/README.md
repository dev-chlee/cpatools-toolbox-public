# sync-test-2026-05-23

**자동화 검증용 임시 skill.** 회계 실무와 무관.

이 skill은 cpatools-toolbox → cpatools-toolbox-public mirror 자동 sync 흐름의 end-to-end 검증을 위해 일시적으로 추가됨. 검증 완료 후 폐기 예정.

## 폐기 일정

검증 통과 시 즉시 `published: false` 토글 + 폴더 삭제. 이 README가 보인다면 아직 검증 진행 중이거나 폐기 누락된 상태.
